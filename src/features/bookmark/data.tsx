import type { Bookmark } from "./types"

// TODO: Add your bookmarks here. Entries take an optional `icon` as JSX and a
// `category` from `BookmarkCategory` in `./types`.
export const BOOKMARKS: Bookmark[] = []

import type { Experience } from "@/features/portfolio/types/experiences"

// TODO: Add your work experience here. `id` must match the `experienceId`
// referenced by `USER.jobs` in `user.ts`.
export const EXPERIENCES: Experience[] = []

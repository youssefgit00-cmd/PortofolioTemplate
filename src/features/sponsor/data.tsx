import type { Sponsor } from "./types"

// TODO: Add your sponsors here. Each entry renders its own inline SVG logo so
// the mark inherits `currentColor` and works in both themes.
export const SPONSORS: Sponsor[] = []
